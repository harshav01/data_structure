#include<bits/stdc++.h>
#include<iostream>
using namespace std;

int main(){
    
    int arr[]={5,2,3,1};
    vector<int>box1;vector<int>box2;
    int n=sizeof(arr)/sizeof(arr[0]);
    sort(arr,arr+n);
    for(int i=0;i<n;i++)
    {
        for(int j=0;j<n;j++)
        {
            if()
        }
    }
    return 0;
}